rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow any authenticated user to read/write all collections
    // This is fine for a college demo project
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
